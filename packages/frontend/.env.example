# Frontend environment variables — copy to `.env` and fill in real values.
#
# Only variables prefixed with VITE_ are exposed to client-side code by Vite.
# Anything without that prefix stays server-side at build time.
VITE_GOOGLE_CLIENT_ID=dev-google-client-id.apps.googleusercontent.com
VITE_API_URL=http://localhost:3001
